server:
  port: 8888
spring:
  application:
    name: configuration-server

  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/financeasist/spring-boot-microservices-workshop
          searchPaths: config

